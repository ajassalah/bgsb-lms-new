import {createClient} from '@/lib/supabase/server';import {z} from 'zod';
export async function POST(req:Request){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const {data:p}=await db.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='super_admin')return Response.json({error:'Forbidden'},{status:403});const parsed=z.object({student_id:z.string().uuid(),course_id:z.string().uuid()}).safeParse(await req.json());if(!parsed.success)return Response.json({error:'Select a student and course'},{status:400});const [{data:student},{data:course}]=await Promise.all([db.from('profiles').select('full_name,email,organization_id').eq('id',parsed.data.student_id).single(),db.from('courses').select('title').eq('id',parsed.data.course_id).single()]);const {data,error}=await db.from('enrollments').insert({student_id:parsed.data.student_id,course_id:parsed.data.course_id,organization_id:student?.organization_id||null,enrolled_via:'manual',status:'pending'}).select('id,status,enrolled_at').single();if(error)return Response.json({error:error.code==='23505'?'Student is already enrolled in this course':error.message},{status:400});return Response.json({id:data.id,student:student?.full_name||'Student',studentEmail:student?.email||'',course:course?.title||'Course',date:data.enrolled_at,status:data.status})}
