import {createClient} from '@/lib/supabase/server';import {z} from 'zod';
async function admin(){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user)return null;const {data:p}=await db.from('profiles').select('role').eq('id',user.id).single();return p?.role==='super_admin'?db:null}
export async function PATCH(req:Request,{params}:{params:{id:string}}){const db=await admin();if(!db)return Response.json({error:'Forbidden'},{status:403});const parsed=z.object({name:z.string().trim().min(2).max(100).optional(),is_active:z.boolean().optional()}).safeParse(await req.json());if(!parsed.success)return Response.json({error:'Invalid update'},{status:400});const values:{name?:string;is_active?:boolean;slug?:string}={...parsed.data};if(parsed.data.name)values.slug=`${parsed.data.name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}-${Date.now().toString(36)}`;const {data,error}=await db.from('categories').update(values).eq('id',params.id).select('name,is_active').single();return error?Response.json({error:error.message},{status:400}):Response.json({name:data.name,active:data.is_active})}
export async function DELETE(_:Request,{params}:{params:{id:string}}){const db=await admin();if(!db)return Response.json({error:'Forbidden'},{status:403});const {count}=await db.from('courses').select('*',{count:'exact',head:true}).eq('category_id',params.id);if(count)return Response.json({error:`This category contains ${count} course${count===1?'':'s'}`},{status:409});const {error}=await db.from('categories').delete().eq('id',params.id);return error?Response.json({error:error.message},{status:400}):Response.json({ok:true})}
