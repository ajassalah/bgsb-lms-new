import {createClient} from '@/lib/supabase/server';import {z} from 'zod';
export async function POST(req:Request){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const parsed=z.object({code:z.string().trim().min(4).max(64)}).safeParse(await req.json());if(!parsed.success)return Response.json({error:'Invalid code'},{status:400});const {data,error}=await db.rpc('redeem_enrollment_code',{p_code:parsed.data.code});if(error)return Response.json({error:error.message},{status:400});return Response.json({enrollment:data})}
