import{notFound}from"next/navigation";import{DashboardShell}from"@/components/dashboard-shell";import{ModuleStudentAssignments}from"@/components/module-student-assignments";import{requireProfile}from"@/lib/auth";import{createAdminClient}from"@/lib/supabase/admin";import{loadModuleStudentAssignments}from"@/lib/module-student-assignments";
export default async function Page({params}:{params:{courseId:string;studentId:string;moduleId:string}}){const p=await requireProfile("instructor"),admin=createAdminClient(),{data:access}=await admin.from("course_instructors").select("course_id").eq("course_id",params.courseId).eq("instructor_id",p.id).maybeSingle();if(!access)notFound();const data=await loadModuleStudentAssignments(params.courseId,params.moduleId,params.studentId);if(!data)notFound();return <DashboardShell role="instructor" name={p.full_name}><ModuleStudentAssignments courseTitle={data.courseTitle} moduleTitle={data.moduleTitle} studentId={params.studentId} studentName={data.studentName} initialRows={data.rows}/></DashboardShell>}
