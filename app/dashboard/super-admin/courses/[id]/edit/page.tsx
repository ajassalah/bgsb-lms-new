import {notFound} from 'next/navigation';import {requireProfile} from '@/lib/auth';import {createClient} from '@/lib/supabase/server';import {SuperAdminShell} from '@/components/super-admin-shell';import {CourseEditForm} from '@/components/course-edit-form';
export default async function EditCourse({params}:{params:{id:string}}){const profile=await requireProfile('super_admin'),db=createClient();const [{data:course},{data:categories}]=await Promise.all([db.from('courses').select('id,title,category_id,status,short_description').eq('id',params.id).single(),db.from('categories').select('id,name').eq('is_active',true).order('name')]);if(!course)notFound();return <SuperAdminShell name={profile.full_name}><CourseEditForm course={course} categories={categories||[]}/></SuperAdminShell>}
