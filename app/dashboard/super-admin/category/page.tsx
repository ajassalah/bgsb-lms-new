import {requireProfile} from '@/lib/auth';import {createClient} from '@/lib/supabase/server';import {SuperAdminShell} from '@/components/super-admin-shell';import {CategoryManagement,type AdminCategory} from '@/components/category-management';
export default async function CategoriesPage(){const profile=await requireProfile('super_admin'),{data}=await createClient().from('categories').select('id,name,is_active,courses(count)').order('name');const categories:AdminCategory[]=(data||[]).map((x:any)=>({id:x.id,name:x.name,active:x.is_active,courseCount:x.courses?.[0]?.count||0}));return <SuperAdminShell name={profile.full_name}><CategoryManagement initialCategories={categories}/></SuperAdminShell>}
