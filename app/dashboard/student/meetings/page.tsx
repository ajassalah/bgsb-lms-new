import { DashboardShell } from "@/components/dashboard-shell"; import { InstructorLiveClasses } from "@/components/instructor-portal-pages"; import { requireProfile } from "@/lib/auth"; import { createAdminClient } from "@/lib/supabase/admin";
export default async function StudentMeetings(){const p=await requireProfile("student"),db=createAdminClient(),{data:links}=await db.from("live_session_students").select("session:live_sessions(id,title,description,thumbnail_url,scheduled_start,scheduled_end,meeting_url)").eq("student_id",p.id);return <DashboardShell role="student" name={p.full_name} email={p.email} avatar={p.avatar_url}><InstructorLiveClasses rows={(links||[]).map((x:any)=>x.session).filter(Boolean).map((x:any)=>({id:x.id,title:x.title,description:x.description||"",thumbnail:x.thumbnail_url,start:x.scheduled_start,end:x.scheduled_end,url:x.meeting_url}))}/></DashboardShell>}
