export function BestCoursesCard({ courses, empty = "No enrollment data available." }: { courses: { title: string; enrollments: number }[]; empty?: string }) { return <section className="min-w-0 overflow-hidden rounded-xl border bg-white p-4 sm:p-6"><h2 className="font-bold text-[#17233c]">Best Courses</h2><p className="mt-1 text-xs text-slate-400">Top five courses by student enrollments</p><div className="mt-5 divide-y">{courses.slice(0,5).map((course,index)=><div key={course.title} className="flex min-w-0 items-center gap-2 py-3 sm:gap-4"><span className="grid size-9 place-items-center rounded-lg bg-navy text-xs font-bold text-white">{index+1}</span><b className="min-w-0 flex-1 truncate text-sm text-navy">{course.title}</b><span className="shrink-0 rounded-full bg-red/10 px-2 py-1 text-[10px] font-bold text-red sm:px-3 sm:text-xs">Enroll · {course.enrollments}</span></div>)}{!courses.length&&<p className="py-8 text-center text-sm text-slate-400">{empty}</p>}</div></section> }
