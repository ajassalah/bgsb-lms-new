import {courses} from '@/lib/demo';import type {Role} from '@/lib/types';
export function DashboardHome({role}:{role:Role}){const student=role==='student', instructor=role==='instructor';const stats=student?[['3','Active courses'],['72%','Best progress'],['2','Upcoming sessions'],['1','Certificate']]:instructor?[['6','My courses'],['184','Students'],['12','To grade'],['3','Live sessions']]:[['1,248','Students'],['38','Courses'],['14','Organizations'],['86%','Completion']];return <><div><h1 className="text-3xl font-bold text-navy">Dashboard</h1><p className="mt-2 text-slate-500">Here’s what’s happening across your learning space.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([n,t])=><div className="card" key={t}><b className="text-3xl text-navy">{n}</b><p className="mt-1 text-sm text-slate-500">{t}</p></div>)}</div><div className="mt-8 card"><div className="flex items-center justify-between"><h2 className="text-xl font-bold text-navy">{student?'Continue learning':'Recent activity'}</h2><button className="text-sm font-semibold text-red">View all</button></div><div className="mt-6 divide-y">{courses.map(c=><div className="grid gap-3 py-5 md:grid-cols-[1fr_200px_60px] md:items-center" key={c.slug}><div><b className="text-navy">{c.title}</b><p className="text-sm text-slate-500">{c.school}</p></div><div className="h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-red" style={{width:`${c.progress}%`}}/></div><span className="text-sm font-semibold">{c.progress}%</span></div>)}</div></div></>}
