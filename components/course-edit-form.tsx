'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {ArrowLeft,Save} from 'lucide-react';import {toast} from 'sonner';
export function CourseEditForm({course,categories}:{course:{id:string;title:string;category_id:string|null;status:string;short_description:string|null};categories:{id:string;name:string}[]}){const [busy,setBusy]=useState(false),router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const res=await fetch(`/api/admin/courses/${course.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(res.ok){toast.success('Course updated');router.push('/dashboard/super-admin/courses');router.refresh()}else{toast.error('Course update failed');setBusy(false)}}return <><button onClick={()=>router.back()} className="mb-5 flex items-center gap-2 text-sm font-semibold text-slate-500"><ArrowLeft className="size-4"/>Back to Course List</button><form onSubmit={submit} className="rounded-xl border bg-white p-6"><h1 className="text-2xl font-bold text-navy">Edit Course</h1><div className="mt-6 grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Course Title<input name="title" defaultValue={course.title} className="field mt-2" required/></label><label className="text-sm font-semibold">Category<select name="category_id" defaultValue={course.category_id||''} className="field mt-2"><option value="">Uncategorized</option>{categories.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label className="text-sm font-semibold">Status<select name="status" defaultValue={course.status} className="field mt-2"><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></label></div><label className="mt-5 block text-sm font-semibold">Short Description<textarea name="short_description" defaultValue={course.short_description||''} className="field mt-2 min-h-28"/></label><div className="mt-6 flex justify-end"><button disabled={busy} className="btn-primary gap-2"><Save className="size-4"/>{busy?'Saving…':'Save Changes'}</button></div></form></>}
