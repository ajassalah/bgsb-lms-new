'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';import {toast} from 'sonner';
export function ResetForm(){const [sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get('email'));const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/login`});if(error)toast.error(error.message);else setSent(true)}return sent?<div className="card">Check your email for a secure reset link.</div>:<form onSubmit={submit} className="card w-full max-w-md"><h1 className="text-2xl font-bold text-navy">Reset your password</h1><p className="my-4 text-sm text-slate-500">We’ll send a password reset link to your account email.</p><input className="field" name="email" type="email" placeholder="you@example.com" required/><button className="btn-primary mt-4 w-full">Send reset link</button></form>}
