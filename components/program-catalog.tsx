'use client';
import {useMemo,useState} from 'react';import {ArrowUpRight,BookOpen,Search,SlidersHorizontal} from 'lucide-react';import type {BgsbProgram} from '@/lib/bgsb-programs';
export function ProgramCatalog({programs}:{programs:BgsbProgram[]}){const [query,setQuery]=useState(''),[level,setLevel]=useState('All levels'),[faculty,setFaculty]=useState('All faculties');const levels=['All levels',...Array.from(new Set(programs.map(x=>x.level))).sort()],faculties=['All faculties',...Array.from(new Set(programs.map(x=>x.faculty))).sort()];const visible=useMemo(()=>programs.filter(x=>(level==='All levels'||x.level===level)&&(faculty==='All faculties'||x.faculty===faculty)&&`${x.title} ${x.overview} ${x.faculty}`.toLowerCase().includes(query.toLowerCase())),[programs,query,level,faculty]);return <><div className="mt-10 grid gap-3 rounded-2xl border bg-white p-4 shadow-soft md:grid-cols-[1fr_220px_260px]"><label className="relative"><Search className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input className="field pl-11" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search programmes..."/></label><label className="relative"><SlidersHorizontal className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><select className="field appearance-none pl-11" value={level} onChange={e=>setLevel(e.target.value)}>{levels.map(x=><option key={x}>{x}</option>)}</select></label><select className="field" value={faculty} onChange={e=>setFaculty(e.target.value)}>{faculties.map(x=><option key={x}>{x}</option>)}</select></div><div className="mt-6 flex items-center justify-between"><p className="text-sm text-slate-500"><b className="text-navy">{visible.length}</b> programmes found</p><p className="text-xs text-slate-400">Synchronized with bgsb.lk</p></div>{visible.length?<div className="mt-6 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{visible.map(c=><a href={c.url} target="_blank" rel="noreferrer" className="group overflow-hidden rounded-2xl border bg-white shadow-soft transition hover:-translate-y-1 hover:shadow-xl" key={c.slug}><div className="relative h-40 bg-gradient-to-br from-navy to-blue-900 p-5"><span className="absolute right-4 top-4 rounded-full bg-white/10 px-3 py-1 text-xs font-bold uppercase text-white">{c.level}</span><span className="absolute bottom-5 left-5 text-xs font-semibold uppercase tracking-wider text-white/65">{c.faculty}</span></div><div className="p-5"><p className="text-xs font-bold uppercase tracking-wider text-red">{c.faculty}</p><h2 className="mt-2 text-xl font-bold leading-snug text-navy">{c.title}</h2><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-500">{c.overview}</p><span className="mt-5 flex items-center gap-1 text-sm font-bold text-red">View official programme <ArrowUpRight className="size-4 transition group-hover:-translate-y-1 group-hover:translate-x-1"/></span></div></a>)}</div>:<div className="mt-8 grid min-h-64 place-items-center rounded-2xl border border-dashed bg-white text-center"><div><BookOpen className="mx-auto mb-3 text-slate-300"/><b className="text-navy">No programmes match your filters</b><p className="mt-1 text-sm text-slate-400">Try a different search, level, or faculty.</p></div></div>}</>}
