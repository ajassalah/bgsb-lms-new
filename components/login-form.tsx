'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';import {roleLabels,type Role} from '@/lib/types';import {toast} from 'sonner';
const choices=(Object.entries(roleLabels) as [Role,string][]);
export function LoginForm(){const [role,setRole]=useState<Role>('student');const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget),db=createClient();const {data,error}=await db.auth.signInWithPassword({email:String(form.get('email')),password:String(form.get('password'))});if(error){toast.error(error.message);setBusy(false);return}const {data:p}=await db.from('profiles').select('role,status').eq('id',data.user.id).single();if(!p||p.status!=='active'||p.role!==role){await db.auth.signOut();toast.error(`This account is not registered as ${roleLabels[role]}.`);setBusy(false);return}router.push(`/dashboard/${p.role.replace('_','-')}`);router.refresh()}
return <div className="w-full max-w-md"><div className="mb-8"><p className="font-bold uppercase tracking-[.2em] text-red">Welcome back</p><h1 className="mt-2 text-4xl font-bold text-navy">Sign in to BGSB</h1></div><div className="mb-6 grid grid-cols-2 gap-2 rounded-2xl bg-slate-100 p-2 sm:grid-cols-3">{choices.map(([r,l])=><button className={`rounded-xl px-2 py-2 text-xs font-semibold ${role===r?'bg-white text-navy shadow':'text-slate-500'}`} onClick={()=>setRole(r)} key={r}>{l}</button>)}</div><form className="space-y-4" onSubmit={submit}><label className="block text-sm font-semibold">Email<input className="field mt-2" name="email" type="email" required/></label><label className="block text-sm font-semibold">Password<input className="field mt-2" name="password" type="password" required/></label><div className="flex justify-end"><a href="/password-forgot" className="text-sm font-semibold text-red">Forgot password?</a></div><button disabled={busy} className="btn-primary w-full">{busy?'Signing in…':`Sign in as ${roleLabels[role]}`}</button></form><p className="mt-6 text-center text-xs text-slate-500">Accounts are issued by BGSB. Public registration is not available.</p></div>}
